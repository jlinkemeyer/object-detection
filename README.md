# object-detection

images.zip contains original training, validation and test images and masks for training and validation images
imgs.zip contains training and validation images and their masks. Also contains 911 augmented images and their corresponding masks
test_results.zip contains test images and their predicted mask (grayscale) based on unet trained with 114 original (non-augmented) images
